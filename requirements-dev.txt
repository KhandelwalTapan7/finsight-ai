# Local development / evaluation only — deliberately NOT installed in the
# production image. mlflow, ragas, datasets, streamlit, torch and boto3
# together add well over a gigabyte and none of them are on the request path.
-r requirements.txt

sentence-transformers==3.2.1
langchain-huggingface==0.1.0

mlflow==2.17.0
ragas==0.2.6
datasets==3.0.1

streamlit==1.39.0
pandas==2.2.3

pytest==8.3.3
pytest-asyncio==0.24.0

boto3==1.35.36
