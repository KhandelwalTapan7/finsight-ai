# --- Core ---
fastapi==0.115.6
uvicorn[standard]==0.25.0
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.9

# --- LLM (Groq = free tier, fast) ---
groq==0.11.0
httpx==0.27.2
langchain==0.3.4
langchain-groq==0.2.0
langgraph==0.2.34

# --- MCP (official SDK) ---
mcp==1.1.0

# --- RAG / vector store ---
qdrant-client==1.11.3
sentence-transformers==3.2.1
langchain-huggingface==0.1.0

# --- Multimodal PDF parsing ---
pymupdf==1.24.11        # page rendering + text
pdfplumber==0.11.4       # table extraction

# --- MLOps ---
mlflow==2.17.0
ragas==0.2.6
datasets==3.0.1

# --- UI ---
chainlit==1.3.2
streamlit==1.39.0
pandas==2.2.3

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- AWS (only used when deploying to prod — optional locally) ---
boto3==1.35.36
