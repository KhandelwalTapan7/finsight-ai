# --- Core ---
fastapi==0.115.6
uvicorn[standard]==0.25.0
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.9

# --- LLM ---
groq==0.11.0
httpx==0.27.2
langchain==0.3.4
langchain-groq==0.2.0
langgraph==0.2.34

# --- MCP (official SDK) ---
mcp==1.1.0

# --- RAG / vector store ---
qdrant-client==1.19.1  # must stay reasonably current with Qdrant Cloud's server
                       # version — v1.19+ servers dropped the old /points/search
                       # REST endpoint (see comment in src/rag/vector_store.py)
# fastembed = ONNX MiniLM, ~100 MB resident. Replaces sentence-transformers
# + torch (~1.5 GB), which OOM-kills small Render instances on first query.
fastembed==0.4.2

# --- Multimodal PDF parsing ---
pymupdf==1.24.11        # page rendering + text
pdfplumber==0.11.4      # table extraction

# --- UI ---
chainlit==1.3.2
